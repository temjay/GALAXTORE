

function AddColor() {
    return (
        <>
            <div className="row mb-5">
                <input type="password" className="rounded border-warning bg-dark text-warning" placeholder="Type in your password.." required />
            </div>
            {/* value={password} onChange={e => setPassword(e.target.value)} */}
        </>
    )
}

export default AddColor
